using UnityEditor;
using UnityEngine;

namespace OfficeWar.Editor
{
    //[CustomPropertyDrawer(typeof(TextAreaAttribute))]
    //public class TextAreaDrawer : PropertyDrawer
    //{
    //    public override void OnGUI(Rect position, SerializedProperty property, GUIContent label)
    //    {
    //        if (property.propertyType == SerializedPropertyType.String)
    //        {
    //            EditorGUI.BeginProperty(position, label, property);

    //            // Use TextArea for string properties
    //            property.stringValue = EditorGUI.TextArea(position, property.stringValue);

    //            EditorGUI.EndProperty();
    //        }
    //        else
    //        {
    //            EditorGUI.LabelField(position, label.text, "Use TextArea with strings only!");
    //        }
    //    }
    //}

    //public class TextAreaAttribute : PropertyAttribute { }
}


